Health tests

Purpose: Service health checks for dependencies: Redis, Pinecone, DB, embedding model, etc.

Notes:
- Use lightweight integration or mocked probes to validate startup and dependency health.
- Include expected health endpoints and typical remediation checks.
